/** Zona Intelbras "sempre liberado" (24/7). */
export const ALWAYS_TIME_ZONE_INDEX = 255;

/** Índice mínimo reservado para turnos customizados (1..max). */
export const MIN_CUSTOM_TIME_ZONE_INDEX = 1;

/** Padrão conservador; ajustável via READER_MAX_TIME_ZONES. */
export const DEFAULT_READER_MAX_TIME_ZONES = 32;
