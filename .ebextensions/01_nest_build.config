# dist/ não vai no zip (está no .gitignore). O EB precisa compilar no deploy.
# Instala devDependencies (@nestjs/cli, typescript, etc.) e gera dist/ antes de iniciar o web.
container_commands:
  01_nest_build:
    command: "npm install --include=dev && npm run build"
